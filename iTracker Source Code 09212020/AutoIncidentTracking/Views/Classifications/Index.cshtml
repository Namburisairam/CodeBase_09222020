@model IEnumerable<IncidentTracking.Models.Classification>
@*
    * ================================================================================
    * MODULE:  Classifications\Index.cshtml
    *
    * PURPOSE:
    * This view supports listing classifications.
    *
    * Copyright:    ©2018 by E2i, Inc.
    * Created Date: 2018-11-02
    * Author:       Brad Robbins (brobbins@e2i.net)
    *
    * --------------------------------------------------------------------------------
    * Revision History:
    * Author           Date        Description
    * Brad Robbins	   2018-11-02  Initial version
    * ================================================================================
*@
@{
    ViewData["Title"] = "Index";
}

<h2>Classifications</h2>
<p class="alert-success">@TempData["Message"]</p>
<p class="alert-danger">@TempData["Error"]</p>

<p>
    <a asp-action="Create" class="btn btn-default">Create New</a>
</p>
<table class="table table-striped table-condensed">
    <thead>
        <tr>
            <th>
                <a asp-action="Index" asp-route-sortOrder="@ViewData["NameSortParam"]">Name</a>
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Name)
                </td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.ClassificationId">Edit</a> |
                    <a asp-action="Delete" asp-route-id="@item.ClassificationId">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>
